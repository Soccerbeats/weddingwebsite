node_modules
.next
.git
.gitignore
README.md
.env*
!.env.example

# Don't include photos and config in the image
public/photos/*
!public/photos/.gitkeep
public/config/site.json

# Docker files — the build context is the repository root, so they live in
# docker/ (this file must stay at the context root to be read by the builder)
docker/

# Development files
.vscode
.idea
*.log
npm-debug.log*
